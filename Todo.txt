↔️↔️↔️↔️↔️↔️↔️↔️↔️↔️↔️↔️↔️↔️↔️↔️

Lab 8

Завдання:
· Розробити програми, використовуючи ітератори та stl контейнери. Не
використовувати алгоритми.
· Основне завдання програми повинне бути реалізовано у вигляді
шаблонної функції.
· Продемонструвати результат роботи із контейнерами, які містять дані
різного типу (включаючи об’єкти власних класів).

✨🔮🪄🎩To Do🎩🪄🔮✨
1. ✅ Зсув елементів: Реалізувати функцію для зсуву елементів масиву
std::array на задану кількість позицій за годинниковою стрілкою. -> EncryptionBox void shiftAlphabetElements

2. ✅ Вставка діапазону: вставте елементи з одного std::vector в інший у
певну позицію за допомогою ітераторів. -> SpyArsenal addMissionTasksCodes

3. ✅ git enc aВидалити середній елемент: видалити середній(і) елемент(и) з
std::deque враховуючи, що черга може містити як парну, так і непарну
кількість елементів. -> SpyArsenal void doorUnblock()


↔️↔️↔️↔️↔️↔️↔️↔️↔️↔️↔️↔️↔️↔️↔️↔️

Lab 9
✨🔮🪄🎩To Do🎩🪄🔮✨

Завдання:
· Розробити програми, використовуючи stl алгоритми, ітератори та
контейнери.
· Кожне завдання потребує використання функціонального об’єкту,
вказівника функції або лямбда-виразу (По одному до завдання).
· Виконати всі 8 завдань для успішного захисту лабораторної роботи.

1.✅ Створити std::list<int> на 20 елементів. Заповнити список рандомно
(випадковим чином). Написати функцію bool isOdd(int n) та
використати std::remove_if для обробки списку. -> SpyArsenal limousinesInitialisation

🔝Шпигуну потрібно лише те, що ділиться на 2. Лімузини, колеса лімузина. Список колес (кількості колес кожної машини) машин.

2.✅ Створити std::arry<string> що містить імена. Написати функцію для
порівняння довжин імен та посортувати список імен за довжиною. -> Garage  void sortCarNames

🔝 Імена людей за якими слідкує шпигун. Імена лімузинів, які шпигун їм надав, що довше ім'я, то вище пріорітет.

3.✅ Створити std::deque<double>, що містить ціни товарів. Створіть
фанктор TaxApplier, який приймає ставку у своєму конструкторі та
продемонструйте обробку кожного елементу контейнера за допомогою
алгоритмів. -> Garage applyMafiaTax а фанктор TaxApplier

🔝 Магазин мафії. Стоп. Лімузини.

4.✅ Для заданого std::set<int> напишіть фанктор WithinRnge(int min, int
max), який використовується разом зі std::count_if, щоб знайти,
скільки елементів потрапляє між цими межами. -> SpyDiary countEventsInRange

🔝План скільки днів до дедлайну місії.

5. Задано std::map<st::string, int>, яка описує предмети та їхню кількість
(уявний склад). Використовуючи алгоритми, написати функцію, яка
покаже елементи, кількість яких більша 100.

🔝Джерело (конверт), дохід (всередині конверту)

6. Задано std::vector<int> чисел. Використати алгоритм, що дозволить
замінити всі від’ємні числа вектор на нуль.

🔝Температура кімнат в домі шпигуна. Холодно = міняєм на 0

7. Створити std::vector<int>. Одночасно обчислити суму елементів та
знайти середнє значення (НЕ використовувати std::accumulte).

🔝Успіх шпигуна, статистика. 100 = успіх, 0 = провал. 

8. Задано текстовий рядок. Написати функцію, для підрахунку кількості
голосних літер в рядку. (Використовуючи алгоритми).

🔝голосних менше 10 -> не іти на таємний мітинг, більше 10 -> іти